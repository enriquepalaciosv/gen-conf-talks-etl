# gen-conf-talks-etl

This project extracts data from the Church API about general conference on demand every time a new event gets fired through an SNS topic. It contains source code and supporting files for a serverless application that you can deploy with the SAM CLI.
