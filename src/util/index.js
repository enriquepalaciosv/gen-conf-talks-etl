const { getSession, getTalkOrder, cleanUri, formatTitle, createSynonym, formatAuthor } = require('./talk');

module.exports = {
    getSession,
    getTalkOrder,
    cleanUri,
    formatTitle,
    createSynonym,
    formatAuthor
};